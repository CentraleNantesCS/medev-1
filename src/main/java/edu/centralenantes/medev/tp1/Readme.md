## Git commands 101

Download the latest changes:

```
git pull
```

Stage the changes:

```
git add .
```

Commit the changes:
```
git commit -m "add: class Plateau closes #1"
```

Push the changes:
```
git push
```
